{
  "name": "medifinder",
  "private": true,
  "version": "1.0.0",
  "description": "Healthcare Appointment & Records System (IFN636) - Monorepo",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "prod": "concurrently \"npm run prod --prefix backend\" \"npm run build --prefix frontend\" && serve -s frontend/build",
    "test:backend": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.10"
  },
  "author": "Medifinder Team",
  "license": "ISC"
}
